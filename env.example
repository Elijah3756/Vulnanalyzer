# Environment Configuration for Vulnerability Analyzer
# Copy this file to .env and customize the values

# ================================================
# Data Paths (Container Paths)
# ================================================
CVE_DATA_PATH=/app/data/cvelistV5/cves
DATABASE_PATH=/app/data/databases/cve_database.db
KEV_FILE_PATH=/app/data/known_exploited_vulnerabilities.json
DOWNLOAD_DIR=/app/data/downloads

# ================================================
# API Configuration
# ================================================
# Get a free API key from: https://nvd.nist.gov/developers/request-an-api-key
# This dramatically increases download speed (50 req/30s vs 5 req/30s)
NVD_API_KEY=

# ================================================
# Application Configuration
# ================================================
LOG_LEVEL=INFO
PYTHONPATH=/app

# ================================================
# Docker Configuration
# ================================================
# Host paths for volume mounting (change these to match your setup)
HOST_DATA_PATH=./data
HOST_CVE_PATH=./data/cvelistV5/cves
HOST_DATABASE_PATH=./data/databases
HOST_CONFIG_PATH=./config
HOST_LOGS_PATH=./logs

# ================================================
# Performance Tuning
# ================================================
# SQLite cache size (in KB, default: 2000 = 2MB)
SQLITE_CACHE_SIZE=10000

# Download retry configuration
MAX_RETRIES=5
RETRY_DELAY=120
MAX_RETRY_DELAY=600

# ================================================
# Container Runtime Options
# ================================================
# Set to 'true' to enable verbose logging
VERBOSE_MODE=false

# Container restart policy
RESTART_POLICY=unless-stopped

# ================================================
# Development Configuration
# ================================================
# Set to 'development' for dev mode
ENVIRONMENT=production

# Enable development features
DEV_MODE=false

# ================================================
# Examples of Usage
# ================================================
# 1. Basic setup:
#    cp env.example .env
#    make docker-build && make setup
#
# 2. Download CVEs with API key:
#    Set NVD_API_KEY above
#    docker run --rm -v vuln_data:/app/data -e NVD_API_KEY=$NVD_API_KEY vuln-analyzer:latest download-cves --recent-days 30
#
# 3. Build database:
#    make create-database
#
# 4. Development mode:
#    make docker-shell
#
# 5. Query database:
#    make db-stats 